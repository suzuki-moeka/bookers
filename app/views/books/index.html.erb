 <h1>Books</h1>

  <table>
       <thead>
        <tr>
         <th>Title</th>
         <th>Body</th>
         <th>仮</th>
        </tr>
      </thead>

      <tbody>
       <tr>
         <% @books.each do |book| %>
         <td><%= link_to book.title, book_path(book.id) %></td>
         <td><%= link_to book.body,book_path(book.id) %></td>
         <td><a href="/books/1"></a>Show</td>
         <td><a href="/books/1/edit">Edit</a></td>
       </tr>
      </tbody>
     <% end %>
</table>

<h2>New book</h2>
<%= form_with model: @book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>

<% end %>

